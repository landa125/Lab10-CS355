<!DOCTYPE html>
<html>
<head>
    <title>Resume - View All</title>
</head>
<body>
<h1>Resumes</h1>
<a href="/resume/add/selectuser">Add New Resume</a> <br />
<table border="1">
    <tr><th>First Name</th>
        <th>Last Name</th>
        <th>Resume Name</th>
        <th>Actions</th></tr>
    <% for(var i=0; resume.length > i; i++ ) { %>
    <tr>
        <td><%= resume[i].first_name %></td>
        <td><%= resume[i].last_name %></td>
        <td><%= resume[i].resume_name %></td>
        <td>
            <a href='/resume/edit/?resume_id=<%= resume[i].resume_id %>'>
                Edit
            </a>
            &nbsp;<a href='/resume/delete/?resume_id=<%= resume[i].resume_id %>'>
                Delete
            </a>
        </td>
    </tr>
    <% } %>
</table>
<% if(locals.was_successful_delete) { %>
<div>
    <p>You Successfully Deleted A Resume!</p>
</div>
<% } %>
</body>
</html>
